# k8s/db-secret.yaml — TEMPLATE ONLY, commit this
# Real values injected by Jenkinsfile via withVault + kubectl create secret --from-literal
apiVersion: v1
kind: Secret
metadata:
  name: ai-python-demo-secret
  namespace: ai-python-demo
type: Opaque
# No stringData here — populated by the pipeline's 'Apply App Secret' stage
