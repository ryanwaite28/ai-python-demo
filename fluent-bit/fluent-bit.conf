# This file has been superseded. Use the environment-specific configs instead:
#
#   fluent-bit.local.conf  — local dev, outputs to the opensearch Docker container
#   fluent-bit.prod.conf   — production, outputs to an external OpenSearch cluster
#
# Both files @INCLUDE common.conf for the shared SERVICE, INPUT, and FILTER blocks.
# They are selected by the docker-compose overlay files:
#
#   docker-compose.local.yml  mounts fluent-bit.local.conf
#   docker-compose.prod.yml   mounts fluent-bit.prod.conf
